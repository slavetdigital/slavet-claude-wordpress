#!/usr/bin/env bash
set -euo pipefail
# Reduce context use: set conservative defaults via environment hints (if supported)
echo "[slavet-claude-wordpress] Session start: context-thrifty mode enabled. Use '/wp audit' to gather fresh evidence." >&2
